<Page
    x:Class="HomeScreenApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HomeScreenApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListBox HorizontalAlignment="Left" Height="480" VerticalAlignment="Center" Width="300" 
                 Background="{ThemeResource ListBoxBackgroundThemeBrush}" FontSize="36">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="40" />
                </DataTemplate>
            </ListBox.ItemTemplate>
            <x:String>💡 Lights</x:String>
            <x:String>🎵 Music</x:String>
            <x:String>💻 WiFi Pswd</x:String>
            <x:String>🌙 Mode: Off</x:String>
            <x:String>📺 TV Control</x:String>
            <x:String>🚪 Entrance</x:String>
            <x:String>⚙️ Settings</x:String>
        </ListBox>
        <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" TextAlignment="Center" Text="Welcome Home" 
                   TextWrapping="Wrap" Width="500" Height="480" FontSize="100" Foreground="{ThemeResource ApplicationForegroundThemeBrush}" />

    </Grid>
</Page>
