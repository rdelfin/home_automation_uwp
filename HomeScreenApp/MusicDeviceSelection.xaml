<Page
    x:Class="HomeScreenApp.MusicDeviceSelection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HomeScreenApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock Text="Select a Device" FontSize="72" HorizontalAlignment="Center" Height="100" VerticalAlignment="Top"
                   Foreground="{ThemeResource ApplicationForegroundThemeBrush}" />
        <ListBox Height="380" Width="800" VerticalAlignment="Bottom" Background="{ThemeResource ListBoxBackgroundThemeBrush}"
                 Name="deviceList">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock FontSize="42" Text="{Binding DeviceName}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ProgressRing x:Name="spinner" HorizontalAlignment="Right" VerticalAlignment="Top" Width="50" Height="50" Margin="0,25,25,0" />
    </Grid>
</Page>
